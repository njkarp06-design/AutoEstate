# Placeholder — exact variables get filled in during Phase 1 (Hermes install/config).
# Self-hosted: we manage our own API keys directly, not the Nous Portal.
# Real values go in a local .env, which is git-ignored and never committed.

ANTHROPIC_API_KEY=
