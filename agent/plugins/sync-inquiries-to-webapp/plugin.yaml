name: sync-inquiries-to-webapp
version: "1.2"
description: Syncs the buyer instance's WhatsApp/Telegram inquiry turns to the AutoEstate reporting webapp (POST /api/inquiries), keyed per buyer conversation, attaching the buyer's resolved contact best-effort. The inbound twin of sync-to-webapp; buyer instance only.
