name: active-listings-context
version: "1.0"
description: Fetches the customer's currently-active listings from the AutoEstate reporting webapp and injects them as LLM context when a message looks like a digest/roundup, re-engagement, status-update, or just-sold request.
