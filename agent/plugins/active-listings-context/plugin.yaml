name: active-listings-context
version: "1.3"
description: Fetches the customer's non-sold listings (ACTIVE and UNDER_CONTRACT, each tagged) from the AutoEstate reporting webapp and injects them as LLM context when a message looks like a digest/roundup, re-engagement, status-update, or just-sold request.
